pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
               echo "Executing your ${BUILD_NUMBER}"
               sh './hello-world.sh'
            }
        }
       stage ("Deploy Pod"){
             steps {
                    sh 'kubectl -f pod.yaml'
                    sh 'kubectl get pods'
             }
        }
    }
}
